---
- name: Extract Lifecycle Environment from All Satellite Hosts
  hosts: all  # Target all hosts
  gather_facts: no
  tasks:
    - name: Get hostvars for each host
      debug:
        var: item.foreman_content_facet_attributes.content_view_environments | map(attribute='lifecycle_environment') | list
      loop: "{{ groups['all'] }}"
